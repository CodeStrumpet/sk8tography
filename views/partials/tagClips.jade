div.container-fluid
	div.sidebar.left
		div.well
			div.clip-filter
				h5 Sidebar
			div.clips
				ul.nav.nav-list
					li(ng-repeat="clip in clips", ng-class="{activeclip: $index==currClipIndex}", ng-click="setCurrentClip(clip)", clips-loaded)
						ul.thumbnails
							li.span.thumbspan
								div.thumbnail
									img(ng-show="!clip.thumbFileName", ng-src="http://img.youtube.com/vi/{{clip.videoSegmentId}}/default.jpg")
									img(ng-show="clip.thumbFileName", ng-src="./images/{{clip.thumbFileName}}")							
						div.right.infoColumn
							span {{clip.videoSegmentId}}
							p There is some really long text
	div.content.right.well
		div(ng-controller='YoutubeCtrl').player-console
			include includes/videoconsole
		div.tagging-console
				